services:
  - type: web
    name: herramienta-precios
    env: python
    buildCommand: bash build.sh
    startCommand: uvicorn servidor:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /opt/render/project/.playwright
